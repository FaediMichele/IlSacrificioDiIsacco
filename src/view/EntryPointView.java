package view;

/**
 * Interface for the entry point for creating the window and all elements for the view.
 *
 */
public interface EntryPointView {

    /**
     * Open the window with generic parameter.
     * @param args generic arguments.
     */
    void openEntryPoint(String[] args);
}
