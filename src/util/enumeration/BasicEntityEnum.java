package util.enumeration;

/**
 * 
 * Basic entity enum.
 *
 */
public enum BasicEntityEnum implements EntityEnum {

    /**
     * is basic entity for status component. 
     */
     FIRE, GAPER, HART_RED, DOOR, BOMB, ENEMY_TEAR_SIMPLE, PLAYER_TEAR, KEY, ROCK, WALL, SIMPLE_ENEMY_MOVABLE, SIMPLE_LOCK_ENTITY;
}
